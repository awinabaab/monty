#include "monty.h"

/**
 * nop_op - Does nothing
 * @stack: Stack
 * @line_number: Line number of the op in the bytcode
 */

void nop_op(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
